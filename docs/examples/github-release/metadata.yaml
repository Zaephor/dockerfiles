name: github-release-example
version_source:
  type: github_releases
  repo: example/example-project
  # prerelease_filter: false           # optional: skip pre-releases (default: false)
  # auth_token_secret: GITHUB_TOKEN    # optional: env var holding a GitHub token

architectures: [amd64, arm64]
